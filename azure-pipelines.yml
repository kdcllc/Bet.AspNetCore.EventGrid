trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:

- task: DotNetCoreCLI@2
  inputs:
    command: restore
    projects: '*.sln'

- task: DotNetCoreCLI@2
  displayName: Build
  inputs:
    command: build
    projects: '*.sln'
    arguments: '--configuration $(BuildConfiguration) --no-restore'

- task: DotNetCoreCLI@2
  inputs:
    command: publish
    projects: '*.sln'
    publishWebProjects: True
    arguments: '-f netcoreapp3.0 -r win-x86 --self-contained true /p:PublishTrimmed=true --configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)/app'
    zipAfterPublish: True

- task: AzureWebApp@1
  inputs:
    azureSubscription: 'DreamSpark'
    appType: 'webApp'
    appName: 'bet-eventgrid'
    package: '$(Build.ArtifactStagingDirectory)/app/**/*.zip'
    deploymentMethod: 'auto'

- task: DotNetCoreCLI@2
  inputs:
    command: publish
    projects: '*.sln'
    publishWebProjects: True
    arguments: '-f netcoreapp2.2 --configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)/func'
    zipAfterPublish: True

- task: AzureFunctionApp@1
  inputs:
    azureSubscription: 'DreamSpark'
    appType: 'functionApp'
    appName: 'bet-dream-func'
    package: '$(Build.ArtifactStagingDirectory)/func/**/*.zip'
    deploymentMethod: 'auto'
